{
  "name": "valetax-mt5-ai-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile UI + Cloudflare Worker AI signal API + MT5 EA scaffold for Valetax MT5.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf:secret": "wrangler secret put APP_TOKEN",
    "devvars": "node scripts/create-dev-vars.mjs",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
