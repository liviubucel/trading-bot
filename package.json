{
  "name": "zebrabyte-trading",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "workers/*",
    "durable-objects/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/contracts -w packages/ctrader-protocol -w packages/market-models -w packages/risk-engine && npm run build -w durable-objects/ctrader-account && npm run build -w workers/api -w workers/risk -w workers/telegram -w workers/news && npm run build -w apps/dashboard",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "wrangler": "^3.60.0"
  }
}
